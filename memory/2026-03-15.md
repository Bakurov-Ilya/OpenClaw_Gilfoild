# 2026-03-15

## 03:53 — Vision-делегирование: анализ ошибки

- **Задача:** Илья попросил описать картинку (кот на рабочем столе)
- **Ошибка #1:** Спавнул researcher БЕЗ `agentId` → subagent запустился с hunter-alpha (текстовая) → vision не сработал
- **Ошибка #2:** Описал ошибку Илье, но не предпринял попытку перезапуска сам — ждал указания
- **Исправление:** Илья подсказал "у ресерчера должна быть healer-alpha". Проверил конфиг, перезапустил с `agentId: "researcher"` → успех за 13 секунд
- **Вывод:** Всегда указывать `agentId: "researcher"` для vision и исследовательских задач. researcher → healer-alpha (multimodal), main → hunter-alpha (text-only)
